submitボタンを探す
const submit = document.getElementById("submit")
これを押したら発火
submit.addEventListner('click', メソッド)

  メソッドに定義すること:
  // フォーム経由で投稿された内容を取得
  // サーバーへリクエストを送信

  // フォームに投稿された内容を取得
  
  Form = document.getElementById("form")
  FormData = new FormData(Form)

  // サーバーへリクエストを送信
  
  まずはXMLHttoRequestオブジェクトを作成
  XHR = new XMLHttpRequest()
  どんなリクエストを送るか定義する
  XHR.open('POST', '/posts', true)
  どんな形式でリクエストを送ってほしいか指定
  XHR.responseType = 'json'
  リクエストを送信する
  XHR.send(FormData)


まとめると

submit_btn = document.getElementById("submit");
submit_btn.addEventListner('click', ()=>{
  Form = document.getElementById("form");
  FormData = new FormData(Form);
  XHR = new XMLHttpRequest();
  XHR.open('POST', '/posts', true);
  XHR.responseType = 'json';
  XHR.send(FormData);
});